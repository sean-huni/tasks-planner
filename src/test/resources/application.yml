spring:
  profiles:
    active: dev
  datasource:
    name: tasks
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2
  application:
    name: Task App

server:
  port: 8080